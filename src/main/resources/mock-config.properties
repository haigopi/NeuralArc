# Deprecated: strategy defaults are centralized in defaults-config.properties.
# Keep this file for backward compatibility with legacy tooling only.
# If a mock profile needs custom values in the future, add only mock-specific overrides.
